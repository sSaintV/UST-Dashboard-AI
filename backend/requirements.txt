# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.135.1
uvicorn[standard]==0.41.0
pydantic==2.12.5
python-dotenv==1.2.2

# ── Vision & inference ────────────────────────────────────────────────────────
# opencv-python-headless: server-safe build (no GUI deps)
opencv-contrib-python==4.13.0.92

# mediapipe: ARM-optimised face detection; Google-designed for edge devices.
# Targets ~30 FPS face detection on Pi 5 CPU.
mediapipe==0.10.32

# onnxruntime: CPU inference for the FERPlus emotion ONNX model.
# Uses all available CPU cores via intra_op_num_threads.
onnxruntime==1.24.3
numpy==2.4.1

# ── HTTP (used by later panels for weather/news) ──────────────────────────────
requests==2.32.5
httpx==0.28.1
